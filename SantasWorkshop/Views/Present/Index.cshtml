@model IEnumerable<SantasWorkshop.Models.Present>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-hover tbody table-striped" >
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Contents.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Wrapping.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Recipient.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Contents.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Wrapping.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Recipient.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PresentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PresentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PresentId })
        </td>
    </tr>
}

</table>
